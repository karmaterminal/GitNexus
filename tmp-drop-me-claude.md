# Code-agent journal — silas/gitnexus-large-repo-support-20260529

Anchoring issue: karmaterminal/GitNexus#1

## Dispatch log

- 2026-05-29T17:35:00+00:00: Workorder + journal created on lothric (silas-seat). Branch pushed. About to dispatch claude session.
- 2026-05-29 (claude session start): Read PRINCE-CODE-AGENT-RUNBOOK in full (1616 lines, figs canon 1507062339). Resolved webhook. Verified worktree state.
- 2026-05-29 (§1 reads): README.md done — CLI ships `gitnexus analyze`, deferred-profile env vars surfaced (GITNEXUS_PROFILE_DEFERRED, *_SLOW_MS, etc). ARCHITECTURE.md done — pipeline DAG is `scan→structure→[md,cobol]→parse→[routes,tools,orm]→crossFile→mro→communities→processes`. Chunked parse-and-resolve uses ~20MB byte-budget chunks. CHANGELOG.md: only goes to 1.5.3 + Unreleased (KuzuDB→LadybugDB migration). package.json: 1.6.5 base. The branch is on 1.6.5 even though figs's eval was on 1.6.6-rc.87/rc.88 (which only had a parse-phase chunked-64+7-worker-pool fix + gRPC-Java consumer fix). The deferred-profile wedge is at "legacy call resolution" stage (after buildHeritageMap), specifically in `processCallsFromExtracted` per env-var docs. Known stall issue #1741 (large Java/Kotlin repos).

- 2026-05-29 (§1 deeper code-walk): For openclaw-class TS/JS repos the picture is different than the workorder's surface framing. Per `MIGRATED_LANGUAGES` set in registry-primary-flag.ts (lines 71-85), **TypeScript, JavaScript, Java, Kotlin, Go, C, C++, PHP, Python, C#, Rust, Ruby, COBOL — ALL the openclaw languages — are scope-resolution-primary**. The legacy `parse-impl` deferred band's `shouldAccumulate` filter (parse-impl.ts:619-633) skips registry-primary files, so `deferredWorker*` arrays are EMPTY for openclaw. The `buildHeritageMap: skipped (no heritage records)` log emits cleanly. Then parse-impl finishes its post-band cleanup (bindingAccumulator.finalize, enrichExportedTypeMap, buildExportedTypeMapFromGraph) and returns. Control moves to `crossFile` phase (bounded — has 3% gap threshold + 2000-file hard cap) then to **`scopeResolution` phase** — which is THE wedge for openclaw, not the legacy deferred band.

- 2026-05-29 (§2 diagnosis): **`runScopeResolution` (scope-resolution/pipeline/run.ts:198) is synchronous**. For openclaw's ~16k TS files (and ~Nk JS files), one call to `runScopeResolution` does ALL of: per-file extract (16k iterations of `extractParsedFile`), finalizeScopeModel (over 16k files), preEmitInheritanceEdges, buildMro, buildWorkspaceResolutionIndex, propagateImportedReturnTypes (SCC-ordered), resolveReferenceSites (over millions of sites), and 4 edge-emit passes — **without yielding to the event loop**. Peak live data during the function: parsedFiles[] (16k × scope trees + localDefs + bindings, ~100KB each → ~1.6GB), source content strings (`files` param held throughout, ~80MB), ScopeResolutionIndexes (bindings/augmentations/referenceSites — multi-100MB), nodeLookup, mroByClassDefId, workspaceIndex, referenceIndex (5M+ ResolvedReferences). Total peak ~5GB + graph itself (~1GB) ≈ matches observed 5.2GB plateau. V8 cannot effectively reclaim memory inside a long synchronous function — explains "frozen IO + spinning CPU + RSS plateau". Prior art for THIS exact wedge class: PR #1800 (commit 2006a3e5) fixed PHP-specific case by (a) workspace-level FQN binding map instead of O(files × classes) per-scope augmentations, and (b) releasing `files.length=0 / contents.clear() / preExtracted entries` *between languages* in scope-resolution-phase. That fix helps cross-language pressure but doesn't help within a single language at 16k+ files — which is openclaw's shape.

